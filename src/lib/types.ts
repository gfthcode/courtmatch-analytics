export type League = 'NBA';
export type SeasonType = 'regular' | 'playoffs';
export type Perspective = 'offense' | 'defense';
export type SampleQuality = 'low' | 'medium' | 'high';
export type Position = 'G' | 'F' | 'C' | 'G-F' | 'F-C' | 'Unknown';
export type Player = { id:string; name:string; chineseName:string; aliases:string[]; shortName:string; teamId:string; teamName:string; teamAbbreviation:string; position:Position; height:string; weight:number; jerseyNumber:string; headshotUrl:string; league:League };
export type Team = { id:string; name:string; chineseName:string; abbreviation:string; logoUrl:string };
export type MatchupRecord = { id:string; offensivePlayerId:string; defensivePlayerId:string; season:string; seasonType:SeasonType; league:League; matchupPossessions:number; points:number; fieldGoalAttempts:number; fieldGoalsMade:number; threePointAttempts:number; threePointMade:number; freeThrowAttempts:number; freeThrowsMade:number; turnovers:number; assists?:number; rebounds?:number; updatedAt:string };
export type MatchupMetrics = { efficiency:number|null; pointsPer100:number|null; fieldGoalPercentage:number|null; threePointPercentage:number|null; turnoverRate:number|null; leagueAverage:number|null; differenceFromLeagueAverage:number|null; sampleQuality:SampleQuality };
export type DisplayMode = 'number' | 'fill';
export type AvatarSize = 'small' | 'medium' | 'large';
export type Filters = { player:string; season:string; type:SeasonType; perspective:Perspective; team:string; opponentTeam:string; position:string; minPossessions:number; quality:string; search:string; metric:string; order:'asc'|'desc'; page:number; displayMode:DisplayMode; avatarSize:AvatarSize };
export type PlayType = 'Isolation'|'Transition'|'PRBallHandler'|'PRRollman'|'Postup'|'Spotup'|'Handoff'|'Cut'|'OffScreen'|'OffRebound'|'Misc';
export type PlayerStats = { playerId:string; season:string; seasonType:SeasonType; gamesPlayed:number; pointsPerGame:number; reboundsPerGame:number; assistsPerGame:number; stealsPerGame:number; blocksPerGame:number; fieldGoalPercentage:number; threePointPercentage:number; freeThrowPercentage:number; trueShootingPercentage?:number; updatedAt:string };
export type PlayTypeRecord = { playerId:string; playType:PlayType; grouping:'offensive'|'defensive'; possessions:number; pointsPerPossession:number|null; percentile:number|null; season:string; seasonType:SeasonType; updatedAt:string };
export type TeamStats = { teamId:string; season:string; seasonType:SeasonType; wins?:number; losses?:number; offensiveRating?:number; defensiveRating?:number; pace?:number; updatedAt:string };
export type DataManifest = { provider:string; status:'live'|'demo'|'stale'|'error'|'unavailable'; league:League; season:string; lastUpdated:string; version:string; playerCount:number; matchupRecordCount:number; playtypeRecordCount:number; teamCount:number; isDemo:boolean; source?:string; dataSource?:string; delivery?:string; unmappedPlayerCount?:number; invalidRecordCount?:number };
export type Dataset = { players:Player[]; teams:Team[]; matchups:MatchupRecord[]; playerStats:PlayerStats[]; playtypes:PlayTypeRecord[]; teamStats:TeamStats[]; manifest:DataManifest; source:string; updatedAt:string; mode:'demo'|'live'; seasons:string[] };
export type Ranking = { player:Player; possessions:number; offensePossessions:number; defensePossessions:number; offense:number|null; defense:number|null; edge:number|null; improvement:number|null; stability:number|null; quality:SampleQuality; rank:number; positionRank:number };
